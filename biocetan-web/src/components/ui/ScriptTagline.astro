---
/**
 * Handwritten accent from the brand mockups. Decorative only: one per site (Home hero),
 * hidden from assistive tech, and hidden on small screens where it crowds the hero.
 */
---

<p
  aria-hidden="true"
  class="hidden -rotate-3 font-script text-3xl leading-tight text-green-300 md:block"
>
  <slot />
</p>
